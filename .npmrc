modules-dir=node_modules
# 不指定 script-shell，使用系统默认 shell（cmd），避免 PATH 里的 bash 解析到 WSL 启动器
# 本地沙箱跑 build：信任项目里的 onlyBuiltDependencies
strict-peer-dependencies=false
auto-install-peers=true
# 允许这些包的 install/postinstall 脚本（pnpm 10+ 推荐写法）
ignore-scripts=false